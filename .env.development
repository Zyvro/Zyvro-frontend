# Chargé par `next dev` uniquement, jamais par un build de production.
# L'origine de l'API n'a plus de repli dans le code (voir src/lib/origin.ts) :
# c'est ici qu'elle vit pour le développement, et nulle part dans le bundle.
NEXT_PUBLIC_API_URL=http://localhost:4102
NEXT_PUBLIC_APP_URL=http://localhost:3000
